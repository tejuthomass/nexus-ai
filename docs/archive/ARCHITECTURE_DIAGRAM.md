# File Upload Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           NEXUS AI APPLICATION                               │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────────┐    ┌─────────────────────┐    ┌──────────────────────┐
│  Left Sidebar    │    │   Main Chat Area    │    │   Right Sidebar      │
│  (Chat History)  │    │   (Messages Only)   │    │   (File Management)  │
└──────────────────┘    └─────────────────────┘    └──────────────────────┘
```

---

## Detailed Component Structure

```
┌───────────────────────────────────────────────────────────────────────────┐
│                          BROWSER WINDOW                                    │
│                                                                            │
│  ┌────────────┐  ┌──────────────────────────────────┐  ┌──────────────┐  │
│  │            │  │                                   │  │              │  │
│  │  [+ New]   │  │  ┌────────────────────────────┐  │  │ ATTACHMENTS  │  │
│  │            │  │  │  User: Hello               │  │  │──────────────│  │
│  │  Recent    │  │  │  AI: Hi! How can I help?   │  │  │              │  │
│  │  ────────  │  │  └────────────────────────────┘  │  │ [Upload PDF] │  │
│  │  Chat 1    │  │                                   │  │              │  │
│  │  Chat 2 ✓  │  │  ┌────────────────────────────┐  │  │ myfile.pdf   │  │
│  │  Chat 3    │  │  │                            │  │  │ ℹ️ Max 10MB   │  │
│  │            │  │  │ Type message here...   [▲] │  │  │              │  │
│  │            │  │  └────────────────────────────┘  │  │ 📄 doc1.pdf  │  │
│  │  [Logout]  │  │                                   │  │ 📄 doc2.pdf  │  │
│  │            │  │                                   │  │              │  │
│  └────────────┘  └──────────────────────────────────┘  └──────────────┘  │
│   260px wide                Flexible                      280px wide      │
└───────────────────────────────────────────────────────────────────────────┘
```

---

## File Upload Flow Diagram

```
                    USER INTERACTION
                           │
                           ▼
         ┌─────────────────────────────────┐
         │  1. Click "Upload PDF" Button   │
         └─────────────────────────────────┘
                           │
                           ▼
         ┌─────────────────────────────────┐
         │  2. Browser File Picker Opens   │
         └─────────────────────────────────┘
                           │
                           ▼
         ┌─────────────────────────────────┐
         │  3. User Selects PDF File       │
         └─────────────────────────────────┘
                           │
                           ▼
         ┌─────────────────────────────────┐
         │  4. JavaScript: Form Submits    │
         │     disableChatInput() called   │
         └─────────────────────────────────┘
                           │
                           ▼
         ┌─────────────────────────────────┐
         │  5. HTMX: POST Request          │
         │     - multipart/form-data       │
         │     - CSRF token included       │
         └─────────────────────────────────┘
                           │
                           ▼
                  ┌────────────────┐
                  │  DJANGO BACKEND │
                  └────────────────┘
                           │
                    ┌──────┴──────┐
                    ▼              ▼
         ┌──────────────┐  ┌──────────────┐
         │  Validate    │  │  Check Size  │
         │  Extension   │  │  < 10MB      │
         └──────────────┘  └──────────────┘
                    │              │
                    └──────┬──────┘
                           ▼
         ┌─────────────────────────────────┐
         │  6. Extract Text from PDF       │
         └─────────────────────────────────┘
                           │
                           ▼
         ┌─────────────────────────────────┐
         │  7. Upload to Cloudinary        │
         └─────────────────────────────────┘
                           │
                           ▼
         ┌─────────────────────────────────┐
         │  8. Save to Database (Document) │
         └─────────────────────────────────┘
                           │
                           ▼
         ┌─────────────────────────────────┐
         │  9. Process for RAG (Pinecone)  │
         └─────────────────────────────────┘
                           │
                           ▼
         ┌─────────────────────────────────┐
         │  10. Render attachments_list    │
         │      HTML partial               │
         └─────────────────────────────────┘
                           │
                           ▼
                  ┌────────────────┐
                  │  HTMX RESPONSE │
                  └────────────────┘
                           │
                           ▼
         ┌─────────────────────────────────┐
         │  11. Update #attachments-list   │
         │      with new HTML              │
         └─────────────────────────────────┘
                           │
                           ▼
         ┌─────────────────────────────────┐
         │  12. enableChatInput() called   │
         │      Chat re-enabled            │
         └─────────────────────────────────┘
                           │
                           ▼
              ┌──────────────────────┐
              │  USER SEES NEW FILE  │
              │  CAN CONTINUE CHAT   │
              └──────────────────────┘
```

---

## State Machine Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                        UPLOAD STATE MACHINE                          │
└─────────────────────────────────────────────────────────────────────┘

    ┌──────────┐
    │  IDLE    │  ← Initial State
    │          │    - Upload button enabled
    │          │    - Chat input enabled
    └────┬─────┘
         │
         │ User clicks "Upload PDF"
         │ & selects file
         ▼
    ┌──────────────┐
    │ FILE_SELECTED│
    │              │  - Filename displayed
    │              │  - Form auto-submits
    └────┬─────────┘
         │
         │ disableChatInput() called
         │
         ▼
    ┌───────────┐
    │ UPLOADING │
    │           │  - Processing indicator shown
    │           │  - Chat input disabled
    │           │  - Submit button disabled
    │           │  - Placeholder: "Processing..."
    └─────┬─────┘
          │
    ┌─────┴──────┐
    │            │
    ▼            ▼
┌─────────┐  ┌────────┐
│ SUCCESS │  │ ERROR  │
│         │  │        │  - Error message shown
│         │  │        │  - Chat remains disabled
└────┬────┘  └────┬───┘    during error display
     │            │
     │            │ enableChatInput() called
     │            │
     └─────┬──────┘
           │
           ▼
    ┌──────────┐
    │ COMPLETE │
    │          │  - New file in list
    │          │  - Chat input enabled
    │          │  - Indicator hidden
    │          │  - Ready for next action
    └────┬─────┘
         │
         │ Persistent State
         │
         ▼
    ┌──────────┐
    │  IDLE    │  ← Back to initial
    │          │
    └──────────┘
```

---

## Template Hierarchy

```
index.html (Main Template)
│
├── Left Sidebar
│   ├── New Chat Button
│   ├── Chat History List
│   │   └── Chat Sessions (loop)
│   └── User Profile & Logout
│
├── Main Chat Area
│   ├── Header (Title, Model Info)
│   ├── Messages Container
│   │   ├── Empty State (conditional)
│   │   └── Messages (loop)
│   │       ├── User Messages
│   │       └── AI Messages
│   ├── Loading Spinner
│   └── Chat Input Form
│       ├── Textarea (message)
│       └── Submit Button
│       └── NO FILE UPLOAD ❌
│
└── Right Sidebar (attachments_sidebar.html)
    ├── Header ("ATTACHMENTS")
    ├── Upload Section
    │   ├── Upload Form
    │   │   ├── Upload Indicator
    │   │   ├── Upload Button ✅
    │   │   ├── Hidden File Input
    │   │   ├── File Label
    │   │   └── Size Limit Info
    │   └── Error Messages
    │
    └── Attachments List (attachments_list.html)
        ├── Document Loop
        │   └── Document Card (link)
        │       ├── PDF Icon
        │       ├── Filename
        │       ├── Timestamp
        │       └── Download Icon
        └── Empty State (conditional)
```

---

## HTMX Communication Flow

```
┌──────────────┐                    ┌──────────────┐
│   BROWSER    │                    │    SERVER    │
│  (Frontend)  │                    │  (Backend)   │
└──────────────┘                    └──────────────┘
       │                                    │
       │  1. Initial Page Load              │
       │────────────────────────────────────>
       │         GET /chat/                 │
       │                                    │
       │  2. Full HTML Response             │
       │<────────────────────────────────────
       │    (includes all components)       │
       │                                    │
       │  3. User Uploads File              │
       │    (clicks Upload PDF)             │
       │                                    │
       │  4. HTMX POST Request              │
       │────────────────────────────────────>
       │    POST /chat/<session_id>/        │
       │    multipart/form-data             │
       │    pdf_file: [binary]              │
       │                                    │
       │    [disableChatInput() called]     │
       │    [Processing indicator shows]    │
       │                                    │
       │                                    │  [Validate]
       │                                    │  [Upload]
       │                                    │  [Save]
       │                                    │  [Process]
       │                                    │
       │  5. Partial HTML Response          │
       │<────────────────────────────────────
       │    attachments_list.html           │
       │    (only updated documents)        │
       │                                    │
       │  6. HTMX Swaps Content             │
       │    Target: #attachments-list       │
       │    [enableChatInput() called]      │
       │    [Indicator hides]               │
       │                                    │
       │  7. User Chats Normally            │
       │────────────────────────────────────>
       │    POST /chat/<session_id>/        │
       │    message: "Hello"                │
       │                                    │
       │  8. Message Partial Response       │
       │<────────────────────────────────────
       │    message.html                    │
       │                                    │
```

---

## Data Flow Architecture

```
┌────────────────────────────────────────────────────────────────────┐
│                        DATA PERSISTENCE LAYER                       │
└────────────────────────────────────────────────────────────────────┘

┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│ PostgreSQL  │    │ Cloudinary  │    │  Pinecone   │
│  Database   │    │ File Storage│    │ Vector DB   │
└─────────────┘    └─────────────┘    └─────────────┘
       │                   │                   │
       │                   │                   │
       ▼                   ▼                   ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  Document   │    │  PDF File   │    │  Embeddings │
│   Metadata  │    │   Binary    │    │   Vectors   │
│             │    │             │    │             │
│ - title     │    │ - URL       │    │ - chunks    │
│ - session   │    │ - public_id │    │ - metadata  │
│ - timestamp │    │ - size      │    │ - ids       │
└─────────────┘    └─────────────┘    └─────────────┘

When user uploads:
1. File → Cloudinary (get URL)
2. URL → PostgreSQL (save metadata)
3. Text → Pinecone (store embeddings)

When user reloads:
1. Query PostgreSQL → get Document records
2. Document.file.url → link to Cloudinary
3. Display in attachments list ✅
```

---

## Component Interaction Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                   COMPONENT INTERACTIONS                         │
└─────────────────────────────────────────────────────────────────┘

    attachments_sidebar.html
            │
            ├─→ Contains Upload Form
            │   │
            │   ├─→ Triggers disableChatInput()
            │   │   │
            │   │   └─→ Modifies Chat Input (index.html)
            │   │       │
            │   │       └─→ textarea.disabled = true
            │   │           button.disabled = true
            │   │
            │   ├─→ Shows Upload Indicator
            │   │   (during processing)
            │   │
            │   └─→ HTMX POST to server
            │       │
            │       └─→ Returns attachments_list.html
            │           │
            │           └─→ Swapped into #attachments-list
            │
            └─→ Includes attachments_list.html
                │
                └─→ Renders Document Cards
                    │
                    └─→ Links to Cloudinary URLs

    index.html
            │
            ├─→ Main Chat Input
            │   │
            │   └─→ Controlled by JavaScript
            │       (disableChatInput/enableChatInput)
            │
            └─→ Includes attachments_sidebar.html
                │
                └─→ Right sidebar content
```

---

## Error Handling Flow

```
┌─────────────────────────────────────────────────────────────┐
│                      ERROR SCENARIOS                         │
└─────────────────────────────────────────────────────────────┘

File Too Large (> 10MB)
    │
    ├─→ Server validates size
    │   └─→ Returns system_message.html
    │       └─→ "❌ File too large. Maximum size is 10MB."
    │           │
    │           └─→ HTMX displays in #attachments-list
    │               └─→ enableChatInput() still called
    │                   └─→ User can retry
    
Invalid File Type (not .pdf)
    │
    ├─→ Server validates extension
    │   └─→ Returns system_message.html
    │       └─→ "❌ Invalid file type. Only PDF files are allowed."
    │           │
    │           └─→ Displayed to user
    │               └─→ Chat remains functional

Network Error
    │
    ├─→ HTMX detects failure
    │   └─→ Returns system_message.html
    │       └─→ "❌ Upload failed. Please try again."
    │           │
    │           └─→ Chat re-enables
    │               └─→ User can retry

Database Error
    │
    ├─→ Django catches exception
    │   └─→ Logs error
    │       └─→ Returns error message
    │           └─→ User sees friendly message
    │               └─→ Chat remains functional
```

---

## Security Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                           │
└─────────────────────────────────────────────────────────────┘

1. Authentication Layer
   ├─→ @login_required decorator
   └─→ User must be logged in

2. Session Isolation
   ├─→ Documents filtered by session
   ├─→ Sessions filtered by user
   └─→ Users can't access others' files

3. CSRF Protection
   ├─→ CSRF token in form
   ├─→ Django validates token
   └─→ Prevents cross-site attacks

4. File Validation
   ├─→ Extension check (.pdf only)
   ├─→ Size limit (10MB)
   └─→ Content type verification

5. Cloudinary Security
   ├─→ Secure upload API
   ├─→ Unique public IDs
   └─→ Access control

6. Database Security
   ├─→ ORM prevents SQL injection
   ├─→ Parameterized queries
   └─→ Foreign key constraints
```

---

## Performance Optimization

```
┌─────────────────────────────────────────────────────────────┐
│                  PERFORMANCE FEATURES                        │
└─────────────────────────────────────────────────────────────┘

1. Partial Updates (HTMX)
   └─→ Only #attachments-list refreshed
       └─→ Not full page reload
           └─→ Faster, smoother UX

2. Database Optimization
   └─→ select_related() for sessions
       └─→ Reduces N+1 queries
           └─→ Faster page load

3. Batch Processing (RAG)
   └─→ Vectors upserted in batches
       └─→ Respects Pinecone limits
           └─→ Prevents timeouts

4. Client-Side State
   └─→ JavaScript manages input state
       └─→ No server round-trip needed
           └─→ Instant feedback

5. Lazy Loading
   └─→ Documents loaded per session
       └─→ Not all user documents
           └─→ Efficient memory usage
```

---

This architecture provides:
✅ Clean separation of concerns
✅ Efficient data flow
✅ Robust error handling
✅ Strong security
✅ Optimal performance
✅ Excellent user experience
