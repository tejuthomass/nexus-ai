"""Custom management commands for the Nexus chat application.

This package contains Django management commands for administrative
tasks that can be run via `python manage.py <command>`.

Commands:
    create_superuser_if_missing: Creates a superuser from environment
        variables if one does not already exist.
"""
